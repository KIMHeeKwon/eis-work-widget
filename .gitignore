# 의존 꾸러미 — package-lock.json 으로 다시 받는다
desktop/node_modules/

# 앱이 돌면서 남기는 기록. 사내 시스템 주소와 이동 경로가 그대로 담긴다
desktop/run.log

# 릴리스에 붙이려고 그때그때 만드는 압축본
eis-work-widget-extension.zip

# 패키징 산출물 — 367MB이고 `npm run package`로 언제든 다시 만든다
dist/
release/

# 팀원에게 건네려고 묶은 꾸러미. release/ 와 extension/ 에서 언제든 다시 만든다
전달용/

# 아이콘도 생성물이다 (make-icon.js가 만든다)
desktop/icon.ico
